using Microsoft.AspNetCore.Mvc;
using System.Net;
using TitanHelp.Data.Models;
using TitanHelp.Services;
using TitanHelp.WebApp.Models;

public class TicketController : Controller
{
    // GET: Home
    public ActionResult Index(Individual, Ticket)
    {
        return View();
    }
 
    [HttpPost]
    public ActionResult Index(Individual, Ticket)
    {
        int ID = Individual.ID;
        string Username = Individual.Username;
        string FirstName = Individual.FirstName;
        string LastName = Individual.LastName;
        string Email = Individual.Email;
        DateTime UserCreationTime
 
        return View();
    }

    [HttpDelete]
    public ActionResult Index()
    {
        [HttpDelete]
        [Route("Ticket/deleteTicket")]
        public ActionResult DeleteTicket(int ticketId)
        {
            if(_ticketService.DeleteTicket(ticketId))
            {
                return Ok(HttpStatusCode.Accepted);
            }
            return BadRequest(ticketId.ToString());
        }
}
